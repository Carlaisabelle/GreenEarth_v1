* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

@font-face {
    font-family: 'Roboto';
    src: url('assests/fonts/roboto_light_macroman/Roboto-Light-webfont.woff') format(woff);
    font-style: normal;
    font-weight: 200;
}

@font-face {
    font-family: 'Roboto';
    src: url('assests/fonts/roboto_lightitalic_macroman/Roboto-LightItalic-webfont.woff') format(woff);
    font-style: italic;
    font-weight: 200;
}

@font-face {
    font-family: 'Roboto';
    src: url('assests/fonts/roboto_boldcondensed_macroman/RobotoCondensed-Bold-webfont.woff') format(woff);
    font-style: normal;
    font-weight: 700;
}

:root {
    --primary: #5cbd2f;
    --secondary: #5ca2f8;
}

body {
    font-family: 'Roboto', sans-serif;
    font-size: 18px;
    line-height: 1.3em;
    color: #000000;
    display: flex;
    flex-direction: column;
    margin: 0% 3%;
}

h1 {
    @font-face {
        font-family: 'Roboto';
        src: url('roboto_bold_macroman/Roboto-Bold-webfont') format(woff);
        font-style: normal;
        font-weight: 500;
    }
}

h2 {
    @font-face {
        font-family: 'Roboto';
        src: url('roboto_medium_macroman/Roboto-Medium-webfont') format(woff);
        font-style: normal;
        font-weight: 250;
    }
}

p {
    line-height: 2em;
    margin-left: 2em;
    margin-right: 2em;
}

.header {
    display: flex;
    width: 100%;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    height: 120px;
}

.menu {
    margin: 15px 15px;
    font-size: 1.2em;
}

.menu__item {
    list-style-type: none;
}

.logo {
    margin: 15px 15px;
}

.main-page {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 5%;
}

.homepage {
    display: flex;
    flex-direction: column;
    justify-content: center;
    margin: 10px 10px;
    margin-bottom: 100px;
}

.main-page__header,
.main-page__paragr {
    text-align: center;
    line-height: normal;
    font-size: 1em;
}

.main-logo {
    display: inline-block;
    overflow: hidden;
    margin-bottom: 6em;
}

.call-to-action {
    text-align: center;
    line-height: 0.5em;
    font-size: 1.5em;
}

.call-to-action__first-line {
    color: var(--primary);
    line-height: 1em;
    font-weight: 700;
    font-size: 1.5em;
}

.call-to-action__last-line {
    color: var(--secondary);
    line-height: 1em;
    font-weight: 700;
    font-size: 1.5em;
    margin-top: 0.5em;
    margin-bottom: 0.5em;
}

.about {
    line-height: 2em;
}

.info {
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    justify-content: flex-start;
    align-items: center;
    align-content: center;
    margin-left: 15%;
    margin-right: 15%;
}

.info__item {
    margin: 1em;
    line-height: 1.5em;
}

.other-check {
    display: inline-grid;
    border: 1px solid var(--primary);
    grid-template-rows: auto;
    grid-template-columns: auto auto;
    justify-items: start;
}

.sub {
    width: 6em;
    height: 2em;
    border: var(--primary);
    background-color: var(--primary);
    margin: 1em;
    font-size: 0.9em;
}

.project-container {
    display: grid;
    grid-template-rows: [higher] auto [lower] auto [even-lower] auto [floor] auto [ground];
    grid-template-columns: [begin] auto [finish];
    justify-content: center;
    align-content: center;
}

.theproject {
    display: inline-grid;
    grid-template-rows: [first] 2em [second] auto [third] max-content [forth];
    grid-template-columns: [on-left] 1fr [on-right];
    align-content: center;
    justify-content: center;
}
#title {
    grid-row: first;
    grid-column: on-left;
}
#image {
    grid-row: second;
    grid-column: on-left;
}
#descript {
    grid-row: third;
    grid-column: on-left;
}

.theproject__title {
    justify-self: center;
}
.theproject__image {
    overflow: hidden;
    justify-self: center;
    border: 1px solid var(--primary)
}
.theproject__descript {
    margin-bottom: 2.5em;
}

.contacts__item {
    p {
        line-height: 1.3em;
    }
    margin-bottom: 1em;
}

.contacts :last-child {
    margin-bottom: 0;
}

.info,
.about,
.project-container,
.contacts {
    margin-top: 4em;
    margin-bottom: 6em;
}

.footer {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-content: flex-end;
    align-items: center;
}
.copy {
    margin: 0;
}

a:link {
    text-decoration: none;
    color: var(--secondary);
}

a:hover {
    text-decoration: underline;
    color: green;
}

a:active {
    text-decoration: none;
    color: var(--primary);
}

a:visited:active {
    text-decoration: none;
    color: var(--primary);
}

@media only screen and (min-width: 750px) {
    .main-logo {
        max-width: 50vw;
    }
    .menu__nav {
        display: inline-flex;
        flex-direction: row;
    }
    .menu__item {
        padding-left: 10px;
        margin-left: 50px;
    }
    .contacts {
        margin-left: 25%;
        margin-right: 20%;
    }
    .contacting {
        margin-left: 25%;
        margin-right: 25%;
    }
}

@media only screen and (max-width: 749px) {
    .info {
        margin-left: 5%;
        margin-right: 5%;
    }
}